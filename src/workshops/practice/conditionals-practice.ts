export function isEven(num: number): boolean {
    //use a conditional statement to evaluate if the number is even.
    const remainder = num % 2;

    return true;
}

export function getDayOfTheWeek(dayNum: number): string {
    //use a switch statement to return the days string name based on a number.
    return 'day of the week'
}

export function classifyLetterGrade(grade: number): string {
    // use 'else if' statements to return the correct letter grade
    
    // A = 90-100
    // B = 80-89
    // C = 70-79
    // D = 60-69
    // F = 0-59

    return 'A';
}