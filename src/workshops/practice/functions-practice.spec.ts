import { findTheRemainder } from './functions-practice';

describe("function tests", () => {
    it("should find the remainder of two numbers when divided", () => {
        // expect(findTheRemainder(10,3)).toBe(1);
    })
})