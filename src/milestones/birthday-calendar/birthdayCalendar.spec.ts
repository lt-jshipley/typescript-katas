import { birthdayCalendar } from './birthdayCalendar'

describe('Birthday Calendar', () => {
  it('It is a Placeholder Test', () => {
    const result = birthdayCalendar();
    expect(result).toEqual(true);
  });
})