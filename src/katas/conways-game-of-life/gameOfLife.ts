import { cleanPrint } from '@src/util/console.helpers';

export function performKata(): void {
  cleanPrint('Hello Game of Life.');
  cleanPrint('Hello Game of Life.');
  cleanPrint('Hello Game of Life.');
  cleanPrint('Hello Game of Life.');
  cleanPrint('Hello Game of Life.');
}

export function gameOfLife(): boolean {
  return true;
}